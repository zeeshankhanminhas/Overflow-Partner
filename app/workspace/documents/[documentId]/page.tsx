import Link from 'next/link';
import { requireUserContext } from '@/lib/auth/context';
import { getDocumentById } from '@/lib/repositories/documents';
export default async function Page({params}:{params:Promise<{documentId:string}>}){const {documentId}=await params;const {supabase,organisationId}=await requireUserContext();const doc=await getDocumentById(supabase,organisationId,documentId);return <section><div style={{display:'flex',justifyContent:'space-between',gap:16,alignItems:'center'}}><div><p className="eyebrow">Controlled document</p><h1>{doc.title}</h1></div><Link className="button secondary" href="/workspace/documents">Back</Link></div><article className="card" style={{marginTop:24,padding:40}}><div style={{display:'flex',justifyContent:'space-between',gap:20,borderBottom:'1px solid #ddd',paddingBottom:20}}><div><strong style={{fontSize:24}}>Overflow Partner</strong><p>Engineering capacity, controlled.</p></div><div style={{textAlign:'right'}}><strong>{doc.reference}</strong><p>Version {doc.version} · {doc.status.replaceAll('_',' ')}</p></div></div><div style={{padding:'48px 0'}}><p className="eyebrow">{doc.document_type}</p><h2>{doc.title}</h2><p className="lede">This controlled record is linked to the Overflow Partner operating workflow. Use the browser print function to issue or save this preview as PDF while generated file storage is connected.</p></div><div style={{borderTop:'1px solid #ddd',paddingTop:20}}><p>Created: {new Date(doc.created_at).toLocaleString('en-GB')}</p><p>Document ID: {doc.id}</p></div></article></section>}
