'use client';
import PendingActionForm from '@/components/workspace/PendingActionForm';
import { createProjectDeliveryItemAction } from './actions';

type Option={id:string;label:string};
export default function DeliveryItemCreateForm({projectId,members,partners,dependencies,documents}:{projectId:string;members:Option[];partners:Option[];dependencies:Option[];documents:Option[]}){
  return <PendingActionForm action={createProjectDeliveryItemAction} className="stack"><input type="hidden" name="project_id" value={projectId}/><div className="grid gap-4 md:grid-cols-2"><label>Type<select name="item_type" defaultValue="deliverable"><option value="deliverable">Deliverable</option><option value="milestone">Milestone</option><option value="dependency">Dependency</option></select></label><label>Title<input name="title" required/></label><label>Owner<select name="owner_id" defaultValue=""><option value="">Unassigned</option>{members.map(option=><option key={option.id} value={option.id}>{option.label}</option>)}</select></label><label>Execution partner<select name="partner_id" defaultValue=""><option value="">No partner</option>{partners.map(option=><option key={option.id} value={option.id}>{option.label}</option>)}</select></label><label>Due date<input name="due_date" type="date"/></label><label>Priority<select name="priority" defaultValue="normal"><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="critical">Critical</option></select></label><label>Revision<input name="revision" placeholder="e.g. P01"/></label><label>Depends on<select name="depends_on_id" defaultValue=""><option value="">No dependency</option>{dependencies.map(option=><option key={option.id} value={option.id}>{option.label}</option>)}</select></label></div><label>Description<textarea name="description" rows={2}/></label><div style={{display:'flex',gap:18,flexWrap:'wrap'}}><label style={{display:'flex',alignItems:'center',gap:8}}><input style={{width:'auto'}} type="checkbox" name="internal_review_required"/> Internal review required</label><label style={{display:'flex',alignItems:'center',gap:8}}><input style={{width:'auto'}} type="checkbox" name="client_review_required"/> Client review required</label></div><label>Linked document<select name="linked_document_id" defaultValue=""><option value="">No linked document</option>{documents.map(option=><option key={option.id} value={option.id}>{option.label}</option>)}</select></label><label>Notes<textarea name="notes" rows={3}/></label><button className="button">Add to delivery plan</button></PendingActionForm>;
}
